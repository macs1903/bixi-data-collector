name: Bixi Collector

on:
  schedule:
    - cron: "*/1 * * * *"  # toutes les minutes pour debug
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Debug cron
        run: |
          echo "=== Cron job started ==="
          echo "Date UTC : $(date -u)"
          echo "Date locale : $(date)"
          echo "Current branch: $(git branch --show-current)"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests pandas

      - name: Run Bixi collection
        run: |
          echo "=== Running Python script ==="
          python collect_bixi.py
          echo "=== Script finished ==="

      # Étape de commit et push à activer après avoir confirmé que cron fonctionne
      # - name: Commit and push
      #   env:
      #     GH_PAT: ${{ secrets.GH_PAT }}
      #   run: |
      #     git config --global user.name "macs1903"
      #     git config --global user.email "macs1903@users.noreply.github.com"
      #     git remote set-url origin https://macs1903:${GH_PAT}@github.com/macs1903/bixi-data-collector.git
      #     git add bixi_realtime.csv
      #     git commit -m "Cron update [skip ci]" || echo "No changes"
      #     git push origin HEAD:main
